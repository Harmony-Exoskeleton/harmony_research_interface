project('Harmony Research', 'cpp',
  version : '0.2.0')

cpp = meson.get_compiler('cpp')

inc = include_directories('include')

libdir = meson.current_source_dir() + '/lib/'
subdir('tests')
subdir('tools')
